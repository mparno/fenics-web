The files 'fenics.bib' and 'fenics.rst' should not be edited
directly. Instead, they must be generated from the common input
file 'fenics.pub'.

To generate the files, run the script

  ./generate_bibliography.sh
